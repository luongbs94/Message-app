<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.android.message.menu"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        <activity
            android:name="app.android.message.menu.Login"

            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="app.android.message.menu.MainActivity"

            >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value="info.androidhive.slidingmenu.Login" />
        </activity>
   		<activity
            android:name="app.android.message.menu.HomeFragment"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="app.android.message.menu.FindPeopleFragment"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="app.android.message.menu.AddFriend"
            android:label="@string/app_name" >
        </activity>
        <provider android:name="FriendProvider" 
           android:authorities="list.friend.provider">
        </provider>
    </application>

</manifest>
